services:
  postgres:
    image: postgres:16
    container_name: montreal-intelligence-db
    environment:
      POSTGRES_DB: montreal_intelligence
      POSTGRES_USER: gian
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: